def make_shirt(size, message):
    """Make a shirt.

    Args:
        size (str): The size of the shirt.
        message (str): The message to print on the shirt.

    Returns:
        str: A description of the shirt.
    """
    return f"A {size} shirt with the message '{message}'."